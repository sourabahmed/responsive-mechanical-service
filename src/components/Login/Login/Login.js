import React from 'react';

const Login = () => {
    return (
        <div>
            <h2>Please log in</h2>
            <button className="btn btn-danger">Google sign in</button>
        </div>
    );
};

export default Login;